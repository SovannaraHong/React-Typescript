import Spread from "./Spread";

const MainSpread = () => {
  return (
    <div>
      <Spread type="password" text="click Me Now.." placeholder="input me" />
    </div>
  );
};

export default MainSpread;
